import React from 'react';

const Header = React.memo(() => (
  <header className="header">
    <h1 className="heading">Typing Master</h1>
    <p className="subheading">Become a typing champion</p>
  </header>
));

export default Header;
